# 项目进度总结 - 2025/12/19

## 一、项目基本信息

### 项目名称
**Personal Map Site** - 个人作品集/简历网站（神话雕刻风格）

### 项目定位
基于 Astro 的静态站点生成器（SSG），采用**神话雕刻（Mythic Engraving）**设计系统，以交互式地图为核心，展示个人经历、作品、决策日志等内容。

### 核心设计理念
- **视觉风格**：羊皮纸背景 + 墨色系统 + 神话雕刻美学
- **交互方式**：地图探索式导航 + 光束连接 + 建筑隐喻
- **内容组织**：多个独立"建筑"对应不同内容模块

---

## 二、技术架构总览

### 核心技术栈
```json
{
  "框架": "Astro 5.16.5",
  "样式": "Tailwind CSS 4.1.18",
  "类型检查": "TypeScript",
  "构建工具": "Vite",
  "部署": "静态站点生成 (SSG)"
}
```

### 关键依赖
```bash
npm install astro@^5.16.5
npm install tailwindcss@^4.1.18 @tailwindcss/vite@^4.1.18
```

### 项目结构
```
F:\resume\personal-map-site\
├── public/
│   ├── map/                      # 地图相关 SVG 资源
│   │   ├── background.webp       # 背景图
│   │   ├── core_building.svg     # 中心建筑
│   │   ├── workshop_building.svg # AI 实验坊建筑
│   │   ├── workshop_sign.svg     # AI 实验坊标牌
│   │   ├── knowledge_building.svg # 知识塔建筑
│   │   ├── knowledge_sign.svg    # 知识塔标牌
│   │   ├── archives_building.svg # 古卷库建筑
│   │   ├── archives_sign.svg     # 古卷库标牌
│   │   ├── command_building.svg  # 指挥所建筑
│   │   ├── command_sign.svg      # 指挥所标牌
│   │   ├── resume.svg            # Resume 标识
│   │   └── timeline/             # 时间轴年份徽章
│   │       ├── 2022.svg
│   │       ├── 2023.svg
│   │       ├── 2024.svg
│   │       ├── 2025.svg
│   │       └── 2026.svg
│   └── girl/
│       └── girl.svg              # 小人角色引导
│
├── src/
│   ├── components/
│   │   ├── Command/
│   │   │   └── StatsPanel.astro
│   │   ├── Log/
│   │   │   └── QuestLogList.astro
│   │   ├── Map/
│   │   │   ├── MapNode.astro
│   │   │   └── WorldMap.astro
│   │   ├── Workshop/
│   │   │   └── CrystalCard.astro
│   │   ├── EntryCard.astro
│   │   └── GalleryFilters.astro
│   │
│   ├── content/                  # Astro Content Collections
│   │   ├── command-center/       # ✅ 决策中心文章
│   │   │   ├── 2025-01-design-for-agency-not-convenience.md
│   │   │   ├── 2025-02-build-vs-integrate-tradeoffs.md
│   │   │   ├── 2025-03-system-thinking-hidden-costs.md
│   │   │   └── 2025-12-19-gpt-summary-of-my-2025.md
│   │   ├── papers/               # 论文/实验
│   │   ├── courses/              # ComfyUI 课程
│   │   ├── reflections/          # 生活思考
│   │   ├── archives/             # 古卷库（空）
│   │   ├── articles/             # 通用文章（空）
│   │   ├── misc/                 # 杂项（空）
│   │   └── config.ts             # Content Collection 配置
│   │
│   ├── layouts/
│   │   └── BaseLayout.astro      # 基础布局
│   │
│   ├── pages/
│   │   ├── index.astro           # 🗺️ 首页（交互式地图）
│   │   ├── resume.astro          # 📄 简历页（带时间轴）
│   │   ├── command.astro         # 🎯 Command Center 概览页
│   │   ├── command-center/
│   │   │   ├── index.astro       # 决策日志列表页
│   │   │   └── [slug].astro      # 决策日志详情页
│   │   ├── log.astro             # 📚 知识塔列表页
│   │   ├── log/
│   │   │   └── [collection]/[...slug].astro  # 知识塔详情页
│   │   ├── workshop.astro        # ⚗️ AI 实验坊列表页
│   │   ├── workshop/
│   │   │   └── formy.astro       # AI 实验坊详情示例
│   │   └── archives.astro        # 🗃️ 古卷库列表页
│   │
│   ├── scripts/
│   │   └── girl-runner.ts        # 小人跟随动画脚本
│   │
│   └── styles/
│       ├── global.css            # 全局样式
│       └── tokens.css            # 设计 Token（颜色、字体）
│
├── astro.config.mjs              # Astro 配置
├── package.json                  # 项目依赖
├── tsconfig.json                 # TypeScript 配置
└── tailwind.config.js            # Tailwind 配置
```

---

## 三、本地开发环境

### 启动命令
```bash
cd F:\resume\personal-map-site
npm run dev
```

### 当前运行端口
**http://localhost:4327/**

> **注意**：端口 4321-4326 已被占用，系统自动使用 4327

### 常用命令
```bash
# 开发服务器
npm run dev

# 生产构建
npm run build

# 预览构建结果
npm run preview

# 清除 Astro 缓存（当出现 404 或内容更新不生效时）
rm -rf .astro
npm run dev
```

### 浏览器调试技巧
- **硬刷新**：`Ctrl + Shift + R` 或 `Ctrl + F5`（清除缓存）
- **开发者工具**：`F12`
- **响应式模式**：`Ctrl + Shift + M`

---

## 四、首页（Map 首页）当前状态

### 文件位置
`src/pages/index.astro`

### 核心功能
1. **交互式地图**：1376x768px 固定设计画布，响应式缩放适配视口
2. **建筑系统**：5个地标建筑 + 1个中心建筑
3. **光束系统**：中心建筑到各地标的能量光束连接
4. **小人引导**：跟随鼠标的小人角色（`girl-runner.ts`）
5. **Resume 标识**：中央底部的简历入口（500x150px）

### 地标配置（核心数据）
```javascript
const landmarks = [
  {
    id: 'workshop',
    route: '/workshop',
    title_en: 'AI Workshop',
    title_zh: 'AI 实验坊',
    building: { src: '/map/workshop_building.svg', ... },
    sign: { src: '/map/workshop_sign.svg', ... },
    beamEnd: { x: 330, y: 300 }
  },
  {
    id: 'knowledge',
    route: '/log',
    title_en: 'Knowledge Tower',
    title_zh: '知识塔',
    building: { src: '/map/knowledge_building.svg', ... },
    sign: { src: '/map/knowledge_sign.svg', ... },
    beamEnd: { x: 1170, y: 200 }
  },
  {
    id: 'archives',
    route: '/archives',
    title_en: 'Ancient Archives',
    title_zh: '古卷库',
    building: { src: '/map/archives_building.svg', ... },
    sign: { src: '/map/archives_sign.svg', ... },
    beamEnd: { x: 240, y: 600 }
  },
  {
    id: 'command',
    route: '/command',
    title_en: 'Command Center',
    title_zh: '指挥所',
    building: { src: '/map/command_building.svg', ... },
    sign: { src: '/map/command_sign.svg', ... },
    beamEnd: { x: 1100, y: 540 }
  }
];
```

### 光束系统技术实现
- **SVG 内联实现**：单一 `<svg class="beam-layer">` 覆盖层
- **三层渐变**：外层金黄光晕 + 中层淡金雾气 + 内层白色芯
- **流动效果**：
  - `stroke-dasharray` + `animation` 实现高光流动
  - `<animateMotion>` + `<circle>` 实现粒子流动
  - 模糊滤镜 `<feGaussianBlur>` 增强柔和感
- **交互触发**：
  - 鼠标悬停中心建筑 → 全部光束激活（延迟80ms逐个显示）
  - 鼠标悬停单个地标 → 仅对应光束激活

### Debug 模式
- **触发方式**：点击右上角 🔧 按钮 或 按 `D` 键
- **显示内容**：
  - 建筑边界（红色虚线）
  - 标牌边界（蓝色虚线）
  - 可点击区域（绿色虚线）
  - 光束端点锚点（橙色圆点）

---

## 五、视觉与交互系统

### 设计 Token（`src/styles/tokens.css`）

#### 颜色系统
```css
:root {
  /* 基础色 */
  --bg-parchment: #efe9c6;    /* 羊皮纸背景 */
  --ink-main: #1f1f1f;         /* 主墨色 */
  --ink-soft: #4a4a4a;         /* 柔和墨色 */
  --ink-faded: #8a8a8a;        /* 褪色墨色 */

  /* 强调色 */
  --accent-glow: rgba(255, 245, 200, 0.6);  /* 发光效果 */
  --accent-beam: rgba(255, 245, 200, 0.3);  /* 光束效果 */
}
```

#### 字体系统
```css
:root {
  /* 英文字体 */
  --font-en-title: 'Cinzel', serif;      /* 标题 */
  --font-en-body: 'Inter', sans-serif;   /* 正文 */

  /* 中文字体 */
  --font-zh-body: 'Source Han Serif', serif;     /* 正文 */
  --font-zh-display: 'HanYiShenGong', serif;     /* 展示标题 */

  /* 传统支持 */
  --font-sans: 'Inter', sans-serif;
  --font-serif: 'Cinzel', serif;
}
```

#### 自定义字体
- **汉仪神工体**：用于中文标题/地点名/牌匾
- 路径：`/fonts/HanYiShenGongTi/HanYiShenGongTiJian-1.woff2`

### 响应式策略
- **桌面端**：固定 1376x768px 画布，视口缩放
- **移动端 (≤768px)**：
  - 时间轴垂直排列
  - 汉堡菜单替代地标直接点击
  - 光束效果隐藏（性能优化）

### 无障碍支持
- **`prefers-reduced-motion`**：禁用所有动画
- **屏幕阅读器**：`.sr-only` 类提供文本描述
- **ARIA 标签**：`aria-label` 用于交互元素

---

## 六、Resume 页面（`src/pages/resume.astro`）

### 当前状态
✅ **已完成** - 2025/12/19 最新调整

### 核心模块

#### 1. 页面头部
```astro
<header class="resume-header">
  <h1 class="page-title">Resume</h1>
  <p class="page-subtitle">个人简历</p>
</header>
```

#### 2. 时间轴（Timeline）
**设计参考**：横向流动，年份徽章 + 光束连接

**当前尺寸配置**：
```css
.timeline-container {
  gap: 4rem;                    /* 容器间距 */
}

.timeline-arrow {
  font-size: 8rem;              /* 箭头尺寸 */
}

.timeline-track {
  height: 360px;                /* 轨道高度 */
}

.year-badge {
  width: 320px;                 /* 年份徽章宽度（桌面） */
}

.timeline-node {
  gap: 1.5rem;                  /* 节点间距 */
}
```

**年份数据**：
- 2022, 2023, 2024, 2025, 2026
- SVG 文件位置：`public/map/timeline/{year}.svg`

**光束效果**（与首页同技术）：
- 外层光晕：`stroke-width: 40`
- 内层芯：`stroke-width: 8`
- 流动高光：`stroke-width: 4`, `stroke-dasharray: 80 160`
- 流动粒子：半径 6/5/4
- 模糊滤镜：`stdDeviation: 16`

**交互效果**：
- Hover：向上浮起 16px + 放大 1.08 倍
- 阴影增强

**移动端适配**：
- 时间轴切换为垂直布局
- 光束效果隐藏（`display: none`）
- 年份徽章缩小至 220px

#### 3. Skills 模块
```astro
<section class="resume-section">
  <h2 class="section-title">Skills</h2>
  <div class="skills-grid">
    <div class="skill-item">
      <span class="skill-name">AI / Vision</span>
      <span class="skill-level">85%</span>
    </div>
    <div class="skill-item">
      <span class="skill-name">Design / Architecture</span>
      <span class="skill-level">80%</span>
    </div>
    <div class="skill-item">
      <span class="skill-name">Product / Systems</span>
      <span class="skill-level">88%</span>
    </div>
  </div>
</section>
```

#### 4. Explore More 导航
快速链接到其他页面：
- Command Center → `/command`
- Decision Logs → `/command-center`
- Knowledge Tower → `/log`
- AI Workshop → `/workshop`

---

## 七、Command Center 系统

### 页面架构
1. **概览页**：`/command` - 展示身份、能力、原则
2. **列表页**：`/command-center` - 所有决策日志
3. **详情页**：`/command-center/[slug]` - 单篇文章

### Content Collection 配置
```typescript
// src/content.config.ts
const commandCenterCollection = defineCollection({
  type: 'content',
  schema: z.object({
    title_en: z.string(),
    title_zh: z.string(),
    date: z.coerce.date(),
    type: z.enum(['decision-log', 'trade-offs', 'system-thinking', 'direction-notes']),
    status: z.enum(['draft', 'published']).default('published'),
    tags: z.array(z.string()).default([]),
    summary: z.string(),
    cover: z.string().optional(),
    featured: z.boolean().optional(),
  }),
});
```

### 文章类型
- `decision-log`：决策日志
- `trade-offs`：权衡分析
- `system-thinking`：系统思考
- `direction-notes`：方向笔记

### 已有文章（6篇）
```
src/content/command-center/
├── 2025-01-decision-log-ai-product-without-illusion.md
├── 2025-01-design-for-agency-not-convenience.md (featured)
├── 2025-02-build-vs-integrate-tradeoffs.md (featured)
├── 2025-02-tradeoff-build-vs-integrate.md
├── 2025-03-system-thinking-hidden-costs.md
└── 2025-12-19-gpt-summary-of-my-2025.md
```

### 重要修复记录
**问题**：点击文章列表显示 404
**原因**：文章原本在 `posts/` 子文件夹，路由无法识别
**解决方案**（2025/12/19）：
```bash
cd src/content/command-center
mv posts/*.md .
rm -rf posts/
```
- 将所有文章移到 `command-center/` 根目录
- 删除空的 `posts/` 文件夹
- 重启开发服务器

---

## 八、Knowledge Tower（知识塔）系统

### 页面架构
1. **列表页**：`/log` - 展示所有内容集合
2. **详情页**：`/log/[collection]/[...slug]` - 动态路由支持多级路径

### Content Collections
```typescript
// src/content.config.ts
const baseSchema = z.object({
  title_en: z.string(),
  title_zh: z.string(),
  date: z.coerce.date(),
  tags: z.array(z.string()).default([]),
  status: z.enum(['draft', 'published']).default('published'),
  cover: z.string().optional(),
  summary: z.string().optional(),
});

// Papers & Experiments
const papersCollection = defineCollection({
  type: 'content',
  schema: baseSchema.extend({
    type: z.literal('paper'),
    venue: z.string().optional(),
    authors: z.array(z.string()).optional(),
  }),
});

// ComfyUI Course
const coursesCollection = defineCollection({
  type: 'content',
  schema: baseSchema.extend({
    type: z.literal('course'),
    lesson_number: z.number(),
    duration: z.string().optional(),
    difficulty: z.enum(['beginner', 'intermediate', 'advanced']).optional(),
  }),
});

// Life Reflections
const reflectionsCollection = defineCollection({
  type: 'content',
  schema: baseSchema.extend({
    type: z.literal('reflection'),
    mood: z.string().optional(),
  }),
});

// Misc
const miscCollection = defineCollection({
  type: 'content',
  schema: baseSchema.extend({
    type: z.literal('misc'),
    category: z.string().optional(),
  }),
});
```

### 集合导出
```typescript
export const collections = {
  'papers': papersCollection,
  'courses': coursesCollection,
  'reflections': reflectionsCollection,
  'misc': miscCollection,
  'command-center': commandCenterCollection,
};
```

### 当前内容
- `papers/`：论文/实验（有内容）
- `courses/`：ComfyUI 课程（有内容）
- `reflections/`：生活思考（空）
- `misc/`：杂项（空，控制台警告）

---

## 九、小人角色引导系统

### 文件位置
- 脚本：`src/scripts/girl-runner.ts`
- 资源：`public/girl/girl.svg`

### 技术实现
```typescript
const MAX_OFFSET = 20;    // 小人距离鼠标的偏移量
const LERP = 0.15;        // 平滑插值系数
const FLIP_INTERVAL = 6;  // 跑步翻转频率（帧）
```

### 动画效果
1. **跟随鼠标**：平滑插值延迟跟随（LERP）
2. **朝向变化**：
   - **移动时**：每 6 帧翻转一次（模拟迈步）
   - **静止时**：朝向鼠标位置
3. **上下 bounce**：`sin` 函数模拟跑步弹跳
4. **Transform 组合**：
   ```javascript
   el.style.transform = `
     translate(${x}px, ${y}px)
     translate(-50%, -50%)
     translateY(${bounce}px)
     scaleX(${currentFacingDirection})
   `;
   ```

### 已知问题
⚠️ **小人当前不显示/不动**（待修复）

可能原因：
1. SVG 路径错误
2. TypeScript 编译问题
3. 脚本未正确加载

---

## 十、已解决的技术问题

### 1. Command Center 文章 404
- **时间**：2025/12/19 14:30
- **症状**：点击文章列表跳转到 `/command-center/posts/...` 显示 404
- **原因**：文章在 `posts/` 子文件夹，但路由文件 `[slug].astro` 不支持嵌套路径
- **解决**：移动所有文章到根目录，删除 `posts/` 文件夹

### 2. Resume Badge 误跳转
- **时间**：2025/12/19 15:10
- **症状**：点击 Resume Badge 跳转到 `/command` 而非独立页面
- **原因**：初始链接设置错误
- **解决**：创建新页面 `/resume`，修改 `href="/resume"`

### 3. Timeline 尺寸调整反复
- **时间**：2025/12/19 15:30 - 16:15
- **症状**：多次调整后徽章显示太小或太大
- **原因**：
  - Flexbox 压缩了容器宽度
  - 设置了过大的 `min-width: 4000px` 导致溢出
  - 浏览器缓存未刷新
- **最终方案**：
  - 年份徽章：320px（桌面）、220px（移动）
  - 容器间距：4rem
  - 移除 `min-width` 限制
  - 使用 flexbox 自动布局

---

## 十一、待办事项（TODO）

### 🔴 高优先级
1. **修复小人角色显示问题**
   - 检查 `girl.svg` 路径是否正确
   - 验证 TypeScript 编译输出
   - 确认脚本在 `index.astro` 中正确引入

2. **完善 Resume 页面内容**
   - 添加工作经历时间线详情
   - 为每个年份节点添加点击交互
   - 设计 modal 或侧边栏展示详细信息

3. **补充空白 Content Collections**
   - `reflections/`：添加生活思考文章
   - `misc/`：添加杂项内容（避免控制台警告）

### 🟡 中优先级
4. **优化光束系统性能**
   - 考虑使用 Canvas 替代 SVG（大量节点时）
   - 添加 `will-change` 属性优化动画
   - 移动端考虑完全禁用光束

5. **增强 Debug 模式**
   - 添加实时坐标显示
   - 支持拖动调整建筑位置
   - 导出坐标配置到 JSON

6. **完善 Archives（古卷库）内容**
   - 确定内容方向
   - 设计独特的视觉风格
   - 添加内容文章

### 🟢 低优先级
7. **SEO 优化**
   - 添加 `meta` 标签
   - 生成 `sitemap.xml`
   - 配置 `robots.txt`

8. **性能优化**
   - 图片懒加载
   - SVG 文件压缩
   - 代码分割

9. **国际化支持**
   - 英文/中文切换
   - 路由 i18n 配置

---

## 十二、常见开发场景

### 场景 1：添加新的 Command Center 文章
```bash
# 1. 创建 markdown 文件
touch src/content/command-center/2025-XX-XX-new-article.md

# 2. 添加 frontmatter
---
title_en: "New Decision Log"
title_zh: "新决策日志"
date: 2025-12-20
type: "decision-log"
status: "published"
tags: ["ai", "product"]
summary: "This is a summary"
featured: true
---

# Your content here

# 3. 重启开发服务器（如果内容不显示）
npm run dev
```

### 场景 2：调整地图建筑位置
```javascript
// 在 src/pages/index.astro 中修改 landmarks 配置
{
  id: 'workshop',
  building: {
    rect: { x: 210, y: 240, w: 240, h: 280 },  // 调整这里
    transform: { scale: 1.4, dx: 0, dy: -140 },
  },
  beamEnd: { x: 330, y: 300 },  // 光束端点也需要调整
}
```

**推荐流程**：
1. 开启 Debug 模式（按 `D` 键）
2. 观察边界框和锚点位置
3. 调整坐标值
4. 刷新页面查看效果
5. 重复直到满意

### 场景 3：添加新的知识塔文章（Papers）
```bash
# 1. 创建 markdown 文件
touch src/content/papers/new-paper.md

# 2. 添加 frontmatter
---
title_en: "Paper Title"
title_zh: "论文标题"
date: 2025-12-20
type: "paper"
status: "published"
tags: ["research", "ai"]
summary: "Paper summary"
venue: "Conference Name"
authors: ["Author 1", "Author 2"]
---

# Your content here

# 3. 访问 http://localhost:4327/log 查看
```

### 场景 4：修改设计 Token
```css
/* src/styles/tokens.css */

:root {
  /* 修改背景色 */
  --bg-parchment: #f5f0db;  /* 更亮的羊皮纸色 */

  /* 修改主墨色 */
  --ink-main: #2a2a2a;

  /* 添加新的强调色 */
  --accent-highlight: rgba(255, 200, 100, 0.8);
}
```

---

## 十三、技术细节备忘

### Astro Content Collections 要点
1. **文件必须在 `src/content/[collection]/` 根目录**
   - ❌ 不支持：`src/content/command-center/posts/article.md`
   - ✅ 支持：`src/content/command-center/article.md`

2. **路由文件命名**
   - `[slug].astro`：单段路径（如 `/article-name`）
   - `[...slug].astro`：多段路径（如 `/category/article-name`）

3. **获取内容**
   ```typescript
   import { getCollection } from 'astro:content';

   const allPosts = await getCollection('command-center');
   const publishedPosts = allPosts.filter(p => p.data.status === 'published');
   ```

### SVG 光束技术要点
1. **使用 `linearGradient` 而非 CSS 渐变**
   - 原因：SVG `stroke` 不支持 CSS `linear-gradient()`
   - 必须使用 `<linearGradient>` + `url(#gradientId)`

2. **`gradientUnits="userSpaceOnUse"`**
   - 确保渐变坐标与画布坐标一致
   - 否则渐变会跟随形状变形

3. **性能优化**
   ```css
   .beam-layer {
     will-change: opacity;
     transform: translateZ(0); /* 启用硬件加速 */
   }
   ```

### 响应式布局要点
```css
/* 固定画布 + 视口缩放 */
.map-canvas {
  width: 1376px;
  height: 768px;
  position: absolute;
  left: 50%;
  top: 50%;
  transform: translate(-50%, -50%) scale(var(--viewport-scale));
}

/* JavaScript 计算缩放比例 */
const scale = Math.min(
  window.innerWidth / DESIGN_W,
  window.innerHeight / DESIGN_H
);
mapCanvas.style.transform = `translate(-50%, -50%) scale(${scale})`;
```

---

## 十四、文件快速索引

### 最常修改的文件
```
src/pages/index.astro              → 地图首页（建筑位置、光束配置）
src/pages/resume.astro             → 简历页（时间轴尺寸）
src/content/command-center/*.md    → 决策日志文章
src/styles/tokens.css              → 设计 Token
src/styles/global.css              → 全局样式
```

### 配置文件
```
astro.config.mjs                   → Astro 配置
package.json                       → 依赖管理
src/content.config.ts              → Content Collection schema
tailwind.config.js                 → Tailwind 配置（如果存在）
```

### 资源文件
```
public/map/                        → 所有地图 SVG 资源
public/map/timeline/               → 时间轴年份徽章
public/girl/girl.svg               → 小人角色
public/fonts/                      → 自定义字体
```

---

## 十五、Debug 清单

### 当页面出现 404
1. 检查文件路径是否正确（绝对路径）
2. 确认路由文件命名（`[slug].astro` vs `[...slug].astro`）
3. 确认 Content Collection 文件在根目录
4. 清除 Astro 缓存：`rm -rf .astro && npm run dev`

### 当内容更新不生效
1. 硬刷新浏览器：`Ctrl + Shift + R`
2. 清除 Astro 缓存
3. 检查 `status: 'published'`
4. 重启开发服务器

### 当 SVG 不显示
1. 检查文件路径（相对于 `public/`）
2. 检查 SVG 文件是否损坏（用浏览器直接打开）
3. 检查 CSS `z-index` 层级
4. 检查 `pointer-events: none` 是否影响可见性

### 当动画不流畅
1. 添加 `will-change: transform`
2. 使用 `transform` 而非 `top/left`
3. 检查是否启用硬件加速：`transform: translateZ(0)`
4. 减少动画元素数量

---

## 十六、项目愿景与设计哲学

### 核心理念
> "Build systems, not demos."
> "构建系统，而非演示"

### 设计原则
1. **Prefer clarity over novelty**
   - 清晰优于新奇

2. **AI should extend, not replace, human agency**
   - AI 应当延展而非取代人的能动性

3. **系统思维优先**
   - 考虑长期演进而非短期炫技

### 交互哲学
- **探索式导航**：用户通过地图"探险"发现内容
- **渐进式信息披露**：从建筑 → 标牌 → 详情页，逐层深入
- **视觉隐喻**：用"建筑"隐喻不同内容模块，增强记忆点

---

## 十七、快速恢复开发上下文

### 当你重新打开 Claude Code 时
1. **启动开发服务器**
   ```bash
   cd F:\resume\personal-map-site
   npm run dev
   ```

2. **打开浏览器**
   访问 http://localhost:4327/

3. **阅读本文档**
   理解当前进度和待办事项

4. **确认优先级**
   从"待办事项"章节选择下一个任务

5. **开启 Debug 模式**
   按 `D` 键，观察当前地图状态

### 快速检查清单
```bash
# ✅ 开发服务器是否正常启动
npm run dev

# ✅ 首页地图是否正常显示
open http://localhost:4327/

# ✅ Resume 页面时间轴是否正常
open http://localhost:4327/resume

# ✅ Command Center 文章是否可访问
open http://localhost:4327/command-center

# ✅ 控制台是否有错误
# 打开浏览器开发者工具（F12）检查
```

---

## 十八、联系与协作

### 项目所有者
- **位置**：本地开发环境 `F:\resume\personal-map-site\`
- **工作模式**：单人开发
- **版本控制**：未配置 Git（建议后续添加）

### 建议下一步
1. **初始化 Git 仓库**
   ```bash
   cd F:\resume\personal-map-site
   git init
   git add .
   git commit -m "Initial commit - Project progress 2025/12/19"
   ```

2. **添加 `.gitignore`**
   ```
   node_modules/
   .astro/
   dist/
   .DS_Store
   .env
   ```

3. **推送到远程仓库**（GitHub / GitLab / Gitee）

---

## 十九、总结

### 当前完成度
- ✅ 技术架构：完整
- ✅ 首页地图：80%（小人角色待修复）
- ✅ Resume 页面：90%（时间轴完成，内容待补充）
- ✅ Command Center：100%（系统完整，可持续添加文章）
- ⚠️ Knowledge Tower：60%（系统完整，部分内容待添加）
- ⚠️ Workshop：50%（页面存在，内容较少）
- ❌ Archives：20%（仅有占位页面）

### 下一个里程碑
1. 修复小人角色显示
2. 完善 Resume 时间轴交互
3. 补充各内容模块的实际文章
4. 性能优化和 SEO 配置
5. 部署到生产环境

---

**文档生成时间**：2025/12/19
**文档版本**：v1.0
**最后更新**：Resume 页面时间轴尺寸调整完成

---

## 附录：关键代码片段

### A1. 地图缩放逻辑
```javascript
// src/pages/index.astro - <script> 部分
function scaleCanvas() {
  const viewportW = window.innerWidth;
  const viewportH = window.innerHeight;
  const scale = Math.min(viewportW / DESIGN_W, viewportH / DESIGN_H);

  if (mapCanvas) {
    mapCanvas.style.transform = `translate(-50%, -50%) scale(${scale})`;
  }
}

scaleCanvas();
window.addEventListener('resize', scaleCanvas);
```

### A2. 光束激活逻辑
```javascript
// src/pages/index.astro - <script> 部分
coreBeacon.addEventListener('mouseenter', () => {
  beamMode = 'all';
  beamGroups.forEach((group, index) => {
    const timeout = setTimeout(() => {
      if (beamMode === 'all') {
        group.classList.add('active');
      }
    }, index * 80);
    activeBeamTimeouts.push(timeout);
  });
});

coreBeacon.addEventListener('mouseleave', () => {
  beamMode = 'none';
  clearAllBeams();
});
```

### A3. Content Collection 查询
```typescript
// 示例：获取所有已发布的 Command Center 文章
import { getCollection } from 'astro:content';

const allPosts = await getCollection('command-center');
const publishedPosts = allPosts
  .filter(post => post.data.status === 'published')
  .sort((a, b) => b.data.date.valueOf() - a.data.date.valueOf());

const featuredPosts = publishedPosts.filter(post => post.data.featured);
```

### A4. 时间轴 SVG 结构
```astro
<!-- src/pages/resume.astro -->
<svg class="timeline-beam-layer" preserveAspectRatio="none">
  <defs>
    <filter id="timelineBlur">
      <feGaussianBlur stdDeviation="16"/>
    </filter>
    <linearGradient id="beamGradient" x1="0%" y1="0%" x2="100%" y2="0%">
      <stop offset="0%" stop-color="#f6d37a" stop-opacity="0.3" />
      <stop offset="50%" stop-color="#fffef5" stop-opacity="0.7" />
      <stop offset="100%" stop-color="#f6d37a" stop-opacity="0.3" />
    </linearGradient>
  </defs>

  <!-- 外层光晕 -->
  <line x1="0" y1="50%" x2="100%" y2="50%"
        stroke="url(#beamGradient)"
        stroke-width="40"
        filter="url(#timelineBlur)"
        class="beam-outer" />

  <!-- 流动粒子 -->
  <circle r="6" fill="#ffffff" filter="url(#timelineBlur)">
    <animate attributeName="cx" from="0%" to="100%" dur="2s" repeatCount="indefinite" />
  </circle>
</svg>
```

---

**END OF DOCUMENT**
